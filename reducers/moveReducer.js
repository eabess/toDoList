import { MOVE_GOAL } from "../actions/types";

const initialState = {
    goals: []
};

const moveReducer = (state = initialState, action) => {
    switch(action.type) {
        
        default:
            return state;
    }
}

export default moveReducer;